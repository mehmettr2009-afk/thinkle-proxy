{
  "name": "thinkle-proxy",
  "version": "1.0.0",
  "description": "Tiny proxy that keeps the Gemini API key secret for the Thinkle app.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18"
  }
}
